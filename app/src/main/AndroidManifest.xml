<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <activity
            android:name=".ui.activities.ViewPlanActivity"
            android:exported="true" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ui.activities.CheckInCustomizedActivity"
            android:exported="true"
            android:label="Check In"></activity>
        <activity
            android:name=".ui.activities.ChatRoomActivity"
            android:exported="true"
            android:label="Check In"/>
        <activity
            android:name=".ui.activities.EditProfileActivity"
            android:exported="true"
            android:label="Edit Profile"/>
        <activity
            android:name=".ui.activities.CheckOutActivity"
            android:exported="true"
            android:label="Check Out"/>
        <activity
            android:name=".ui.activities.NoFacilityActivity"
            android:exported="true"
            android:label="No Facility"/>
        <activity
            android:name=".ui.activities.ExerciseActivity"
            android:exported="true"
            android:label="Exercise"/>
        <activity
            android:name=".ui.activities.SelectSportActivity"
            android:exported="true"
            android:label="Select Sport"/>
        <activity
            android:name=".ui.activities.SelectFacilityCheckInActivity"
            android:exported="true"
            android:label="Select Facility"/>
        <activity
            android:name=".ui.activities.SelectFacilityPlanActivity"
            android:exported="true"
            android:label="Select Facility"/>
        <activity
            android:name=".ui.activities.CheckInNormalActivity"
            android:exported="true"
            android:label="Check In"/>
        <activity
            android:name=".ui.activities.AddPlanActivity"
            android:exported="true"
            android:label="Add Plan"/>
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true"
            android:label="App">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.SignInActivity"
            android:exported="true"
            android:label="Sign In">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".ui.activities.UserActivity"
            android:exported="true"
            android:label="User"/>
    </application>

</manifest>